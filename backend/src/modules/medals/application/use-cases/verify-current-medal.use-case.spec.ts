import { VerifyCurrentMedalsUser } from './verify-current-medal-user';

describe('VerifyCurrentMedalsUser', () => {
  it('should be defined', () => {
    expect(new VerifyCurrentMedalsUser()).toBeDefined();
  });
});
